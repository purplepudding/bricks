apiVersion: v1
kind: Service
metadata:
  name: matchmaking
  namespace: bricks
spec:
  selector:
    app: monolith
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8085
  type: ClusterIP
