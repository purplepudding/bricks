apiVersion: v1
kind: Service
metadata:
  name: matchmaking
  namespace: bricks
spec:
  selector:
    app: matchmaking
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
