version: v2

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX

breaking:
  use:
    - FILE

modules:
  - path: .
    name: buf.build/purplepudding/bricks
