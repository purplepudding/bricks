apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: gateway

build:
  artifacts:
    - image: ghcr.io/purplepudding/bricks/gateway
      ko:
        main: ./cmd/gateway
        dependencies:
          paths:
            - cmd
            - config
            - deploy
            - internal
            - service
            - ../lib
            - ../go.mod
            - ../go.sum

manifests:
  kustomize:
    paths:
      - deploy/k8s/local

portForward:
  - resourceType: service
    resourceName: gateway
    port: 80
    localPort: 8080