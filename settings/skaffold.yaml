apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: settings

build:
  artifacts:
    - image: ghcr.io/purplepudding/foundation/settings
      ko:
        main: ./cmd/settings
        dependencies:
          paths:
            - cmd
            - deploy
            - internal
            - ../lib
            - ../go.mod
            - ../go.sum

manifests:
  kustomize:
    paths:
      - deploy/k8s/base

portForward:
  - resourceType: service
    resourceName: settings
    port: 80
    localPort: 8080